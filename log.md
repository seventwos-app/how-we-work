# Update Log

## 2026-09-17
* **Alignment**: Corrected the MCP transport source links in [Technology Stack](stack.md) to their versioned specifications, and clarified in [Repository Provenance](plans/repository-provenance.md) that schema v0.1 does not yet model review cadence, accountable ownership, or drift checking.

## 2026-09-16
* **Wording**: Refined the closing statement in [How We Work](README.md) to connect collaboration at scale more directly with building the future of Seventwos.

## 2026-09-14 (3)
* **Plan**: Added a "Keeping the record true" section to the [Repository Provenance](plans/repository-provenance.md) plan. A provenance record makes claims only a human can affirm -- purpose, accountable party, disclosure -- and those claims decay as the business changes, so a record carries a review cadence. The correction stays manual on purpose: a record a scheduled job rewrites to "stay current" asserts only that the job ran, which hollows out the exact property the record exists to provide. The convention is therefore to **automate noticing, never asserting** -- report *drift* (review interval lapsed; declared accountable party no longer matching the CODEOWNERS default owner) as a prompt addressed to a person. Two constraints are recorded as convention rather than preference: drift is advisory and must not gate merges, because blocking unrelated pull requests on a lapsed review punishes the wrong change and pressures a rubber-stamp update purely to ship; and drift output carries a rule id and JSON path only, never a field value, so reports stay safe in CI logs, step summaries, and issue bodies.

## 2026-09-14 (2)
* **Plan**: Replaced the [Repository Provenance](plans/repository-provenance.md) plan with a practical MVP: a minimal, dependency-free `.seventwos/provenance.json` (schema v0.1) capturing intent, human direction, agent involvement, review, and source outcome; three disclosure classes (`public`/`internal`/`restricted`); Graphify reconfirmed as an optional derived reader only, its license question already resolved and out of scope.
* **Tooling**: Added `scripts/provenance/validate_provenance.py`, a dependency-free validator that reports violations as a JSON path plus a stable rule id (`PROV001`-`PROV005`) and never the offending value, and `tests/test_validate_provenance.py` covering a valid record, each missing required field, an invalid `outcome.origin`, disallowed sensitive/unsupported fields, and safe (value-free) error messages.
* **CI**: Wired the provenance validator and its unit tests into the existing `test` job in `.github/workflows/ci.yml` -- no new workflow, job, or third-party dependency.
* **Process**: Replaced `.github/pull_request_template.md` with four fields -- Intent, Human direction, Agent/tool involvement (none/assisted/generated-and-reviewed), Verification -- matching the provenance record schema.
## 2026-09-14
* **Bundle Manifest Guardrail**: Extended the OKF validator beyond per-file frontmatter checks. Every non-reserved bundle document must now have both a non-empty `type` and an entry in the root [index](index.md); the index also fails validation when it links to a missing Markdown document. This makes a missing Specification registration a CI failure rather than a review-time omission. Added regression coverage for both missing and stale index entries.
* **Validator Scope Correction**: Fixed the OKF validator's exclusion logic after the CI workflow exposed two boundary defects: `--changed` checked Copilot configuration under `.github/` even though those files are tooling rather than bundle concepts, while `--all` inspected absolute path components and silently excluded this entire checkout because its parent directory is named `.copilot`. Exclusions now operate only on repository-relative paths, `.github/` and `.copilot/` are consistently excluded as tooling in both modes, and a regression test covers the boundary. The full five-document bundle baseline now validates rather than reporting a meaningless `0/0`.
* **Stack Scope Guardrail**: Tightened [Technology Stack](stack.md) with an explicit scope statement after the opening paragraph: the document defines runtime, protocol, storage, and licensing decisions only. Agent rosters, workflow choreography, and prompt policy are intentionally excluded and remain in repository-level collaboration artifacts. This prevents architectural drift back into agnetic-framework/process narration when the specification is revised.
* **Topology Layout Correction**: Restructured the system topology diagram in [Technology Stack](stack.md) after rendering it and inspecting the result rather than trusting a successful render. The `Local Execution Sandbox` sat in the third tier but connected only to the Tauri host core in the first, so the layout engine pulled the tiers apart and rendered them out of order across a wide band of dead whitespace. The sandbox is now nested inside the desktop client subgraph, which is also the more accurate placement — worktrees, the application store, and MCP subprocesses all run on the user's machine, not in the cloud. The third tier is renamed `Cloud Agent Services` to reflect what remains in it, and the standalone native-sync node is folded into the homeserver label now that MSC4186 is served natively.
* **Integration Soundness Review**: Audited [Technology Stack](stack.md) for architectural soundness rather than citation accuracy — asking whether the components as wired would actually function — and corrected six defects. (1) **Out-of-band trust-boundary violation**: the topology carried a direct `Agent Gateway → Desktop UI` edge for task dispatch and diffs, bypassing the Matrix fabric entirely. This placed workspace code outside the E2EE envelope and the room permission model, made agent output invisible to mobile collaborators, contradicted section 5.2's statement that agents post into the room timeline, and required inbound network reachability to a client behind NAT. The edge is removed and the no-out-of-band-channel guarantee is now stated explicitly. (2) **Downstream-only sync path**: all three clients were wired solely to the sliding sync endpoint, which streams events downstream but cannot send messages, distribute keys, or authenticate; Client-Server API edges were added alongside. (3) **Missing push decryption**: encrypted push payloads carry only an event identifier, so the iOS Notification Service Extension — which opens its own SDK client against a shared App Group crypto store — and the Sygnal push gateway that holds the APNs/FCM credentials were both absent and are now documented. (4) **Overstated zero-FFI claim**: the Rust-to-Rust path is genuinely FFI-free, but decrypted events still cross a JSON-serialized Tauri IPC boundary to reach the React UI; the claim is now scoped to the host-to-SDK call and the presentation boundary is stated. (5) **Element X licensing understated**: it is dual-licensed AGPL-3.0 **or** Element Commercial, not AGPL-only, which materially changes whether forking is viable. (6) Clarified that the desktop keeps two SQLite stores by design — the SDK's protocol and crypto state separate from application sessions and DAGs — and recorded the Tauri capability constraint that places the MCP client in the Rust host core rather than the webview. Verification table expanded to 21 rows.
* **Licensing Posture**: Added a Licensing & Reuse Posture section to [Technology Stack](stack.md), resolving the previously unexamined "where applicable and permitted" qualifier in the opening statement. The foundations split cleanly along the boundary that matters: `matrix-rust-sdk` (Apache-2.0), Vodozemac (Apache-2.0), and Tauri v2 (MIT / Apache-2.0) are embeddable in a proprietary client, whereas Element X (AGPL-3.0) and Synapse (AGPL-3.0, or commercial licence from Element) carry network-copyleft obligations. Documented the consequence: Seventwos embeds the permissive tier, treats Element X as an architectural reference rather than a fork, and runs Synapse unmodified to keep the AGPL obligation dormant. Reconciled section 4, which previously described the mobile clients as "built upon Element X", to state that they follow its architecture while embedding the Apache-2.0 engine. Renumbered Fact-Checking & Source Verification to section 7 and expanded it to 16 rows with licence citations. Also added SQLDelight to the Android feature list for symmetry with the platform matrix.
* **Claim Alignment**: Reconciled the [Technology Stack](stack.md) opening statement with the verification table. The intro previously grouped Claude Desktop, GitHub Copilot Desktop, and Element X as products whose frontend frameworks Seventwos adopts, but only the Element X stack is confirmed by a primary source. Element X is now named as the framework anchor, with Claude Desktop and Copilot Desktop cited for the desktop patterns they demonstrably establish — matching the `[DIRECTIONAL]` status those two rows now carry.
* **Deep Fact-Check Correction**: Ran a whole-document verification pass over [Technology Stack](stack.md) and corrected six defects. (1) **Fabricated persistence stacks**: the platform matrix credited Element X iOS with CoreData and Element X Android with Room; neither framework appears anywhere in either repository. Both clients delegate room state to the `matrix-rust-sdk` SQLite state store, with SQLDelight on Android and `KeychainAccess` on iOS. (2) **Obsolete MCP transport**: remote server connectivity cited `SSE` / `HTTP`, but HTTP+SSE was deprecated in MCP revision `2025-03-26` in favour of Streamable HTTP. (3) **Unsubstantiated Copilot Desktop claim**: GitHub does not publish the Copilot Desktop host shell technology, so Tauri v2 is now recorded as a Seventwos design choice rather than a confirmed match, with the verifiable worktree-isolation pattern stated separately. (4) **Claude Desktop citation mismatch**: the cited Anthropic page documents MCP configuration, not client architecture; the row is downgraded accordingly. (5) Added WebKitGTK as the Linux webview and qualified the Tauri resource figures as lightweight-shell baselines rather than production footprints. (6) Pinned the Element X iOS deployment target to iOS 18.5 and anchored MatrixRTC to MSC4143. The verification table now spans 13 rows and carries a status legend distinguishing `[VERIFIED]` primary-source claims from `[DIRECTIONAL]` working assumptions.
* **Fact-Check Correction**: Corrected two verified defects in the [Technology Stack](stack.md) topology found by the `Fact Checker` agent. (1) **E2EE trust boundary**: Vodozemac was depicted as a server-side node exchanging encrypted payloads with the sync layer, contradicting Matrix's client-side encryption model; it now appears inside each client SDK, with the homeserver relaying opaque ciphertext only. (2) **Obsolete sync topology**: the standalone Sliding Sync Proxy (MSC3575) was shut down in November 2024 and dropped by Element X in January 2025; the diagram, platform matrix, and prose now reference Simplified Sliding Sync (MSC4186) served natively by the homeserver. Also narrowed the homeserver label to Synapse (Dendrite lacks native MSC4186), added federation to the fabric tier, and documented the Application Service encryption constraint. Expanded the verification table from 7 to 10 cited rows.
* **Scope Refinement**: Removed the End-to-End Communication Flow sequence diagram from [Technology Stack](stack.md). The section narrated a collaboration workflow rather than specifying technology, and every component it referenced is already covered in the platform matrix, topology, and communications fabric sections. Renumbered Fact-Checking & Source Verification to section 6.
* **Diagram Refinement**: Reworked both Mermaid diagrams in [Technology Stack](stack.md) under the `Technical Visualizer` agent. The system topology now encodes the zero-FFI desktop versus UniFFI mobile asymmetry through stroke styling, labels every protocol edge, styles tier and sub-container boundaries distinctly, and applies semantic node shapes to runtimes, datastores, and external systems. The sequence diagram is now split into three annotated phases (intent capture, implementation, verification) with explicit execution-state notes and a revision loop back to the agent gateway. Both diagrams were render-validated with `mermaid-cli`.
* **Scope Boundary**: Separated the working method from the product specification. [Technology Stack](stack.md) now describes only shipping technology; the agent roster that authors and verifies the bundle is documented solely in `.github/copilot-instructions.md`. Removed the `Platform Architect` agent from this bundle — its deliverables (implementation plans, codepath failure-mode analysis, test strategies) are repository-tier concerns, and the canonical agent remains in the implementation repository.
* **Agent Integration**: Established the `Technical Visualizer` specialist agent (`.github/agents/technical-visualizer.agent.md`, pinned to `Claude Sonnet 5`) and the `mermaid-diagrams` skill (`.github/skills/mermaid-diagrams/SKILL.md`) to elevate system architecture visualization with C4 tiered modeling, Tufte information design, and styled Mermaid schematics in [Technology Stack](stack.md).
* **Update**: Refined frontend framework alignment phrasing in [Technology Stack](stack.md).
* **Addition**: Expanded [Technology Stack](stack.md) to integrate the complete multi-platform ecosystem: Desktop (Tauri v2 + React 19), Mobile (Element X on iOS with SwiftUI and Android with Jetpack Compose sharing `matrix-rust-sdk`), and the decentralized Matrix communications fabric (Matrix 2.0 Sliding Sync, Vodozemac E2EE, Matrix Application Services).
* **Addition**: Established [Technology Stack](stack.md) architectural specification for the Seventwos Desktop Application, benchmarking against Claude Desktop and GitHub Copilot Desktop patterns.
* **Agent Integration**: Mapped over the `Fact Checker` specialist agent (`.github/agents/fact-checker.agent.md`) with shared safety blocks for rigorous web evidence triage and claim verification.
* **Plan**: Added [Repository Provenance](plans/repository-provenance.md), defining a layered per-repository manifest, Git/SBOM/attestation evidence, a generated organization catalog, Graphify's non-authoritative indexing role, and a phased rollout. Corrected the installed Graphify `0.9.61` license classification to Apache-2.0 rather than MIT.
* **Tooling**: Fixed OKF markdown discovery to apply excluded-directory rules only to repository-relative paths. Local checkouts stored beneath a parent `.copilot` directory no longer produce a false-clean `0/0 markdown files` baseline, and the test suite now guards against empty bundle discovery and excluded parent-directory regressions.
* **Tooling**: Rewrote `scripts/okf/validate_okf_markdown.py` to enforce OKF v0.2 strictly and recursively: every non-reserved markdown file must carry a parseable YAML frontmatter block with a non-empty `type`; `index.md`/`log.md` must have no frontmatter, except the bundle-root `index.md`, which may carry frontmatter containing only `okf_version`; the optional `tags`/`status`/`generated`/`verified`/`sources` frontmatter families are shape-checked when present; each `sources[]` entry must declare a non-empty `resource`; and every body footnote label (`[^id]`) must correspond to a `sources[].id`. Added a small in-house YAML-subset parser (no third-party dependency) to support the richer shapes, and a `tests/` unit suite (`python -m unittest discover -s tests`) covering the new rules, wired into the `test` CI job.

## 2026-09-13 (4)
* **Organization**: Moved the [Emergency Cloud Handoff](runbooks/emergency-cloud-handoff.md) runbook into `runbooks/`, keeping bundle entrypoints and the Vision at the repository root.

## 2026-09-13 (3)
* **Runbook**: Bounded the [Emergency Cloud Handoff](runbooks/emergency-cloud-handoff.md) procedure to a 1-hour checkpoint per cloud session -- it must push its state and check in at the hour mark (or on completion) instead of running unbounded, so you can choose to extend or stop. The paired "Emergency Cloud Handoff" automation now also nudges existing handoffs approaching their bound.

## 2026-09-13 (2)
* **Runbook**: Added [Emergency Cloud Handoff](runbooks/emergency-cloud-handoff.md), a procedure for moving busy local agent sessions to cloud sessions when you need to shut down suddenly -- force-push in-flight work, open a cloud session per PR from the pushed branch, hand it a resume prompt, then stop the local session.

## 2026-09-13
* **Workflow**: Added a "Bounded PR Process" section to `.github/copilot-instructions.md` requiring recon before starting, validation before first push, batched review responses, sparing base-branch syncs, a two-cycle/30-minute review bound with blocker reporting, and staged (focused-then-full) validation.
* **Tooling**: Added `.github/pull_request_template.md` mirroring the bounded PR checklist, and excluded it from the OKF `type` frontmatter requirement (tooling config, like `copilot-instructions.md`).
* **CI**: Replaced `.github/workflows/okf-markdown.yml` with `.github/workflows/ci.yml`, which reports the `lint`, `test`, and `security` checks the organization ruleset requires on `main`. The previous workflow was path-filtered to `**/*.md` and used a job name the ruleset didn't recognize, so pull requests stayed blocked on checks that never reported.

## 2026-09-12
* **Initialization**: Established the OKF bundle structure.
* **Update**: Classified [How We Work](README.md) as a Vision.
* **Update**: Clarified the opening principle and the company statement.
