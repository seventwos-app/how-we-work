## Summary

<!-- What changed and why. Link the related issue if there is one. -->

## Checklist

- [ ] Checked for an existing PR/issue covering this change and any open review threads before starting.
- [ ] Implementation is complete and validated locally (e.g. `python scripts/okf/validate_okf_markdown.py --changed` for `.md` changes) before the first push.
- [ ] Reviewer feedback, if any, was addressed in a single batched follow-up rather than one push per comment.
- [ ] Branch was synced with `main` only right before this (expected final) push.
- [ ] If review isn't converging after two automated review cycles or ~30 minutes, a concrete blocker is reported below instead of continuing to iterate.

## Blockers / open questions

<!-- Leave "None" if there aren't any. -->
